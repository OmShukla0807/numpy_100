random library has useful functions whose name you can get by 
matches = [x for x in dir(np.random) if "rand" in x.lower()]
the common and interestion ones are random , randint and randn 

some function serve similar purpose externally but use different  internal mechanism(in terms of number distribution) like some uses bell curve 
or otehr distribution like normal 

padding: which can be done through np.pad is widely used where we just border to the existing matrix an example i wnet throgh is CNN(convolution neural network!)

There are some useful function related diagonal 
['diag', 'diag_indices', 'diag_indices_from', 'diagflat', 'diagonal', 'fill_diagonal']


Integer array indexing allow us to just pass array ad index and columns and correspodning values are prited!